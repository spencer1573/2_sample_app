 <!--i think provide is like setting an attribute of the page.-->
<% provide(:title, "Edit user") %>
<% provide(:button_text, 'Save changes') %>
<h1>Update your profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <!-- when form_for(@user) is called
         rails always askes @user.new_record?
         if it is a new record then the form
         is for creation of a new one (true)
         rails will then POST
         if it is already in the database then
         it goes through new record == (false)
         rails will then PATCH -->
    <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <!-- you might forget that target="_blank" opens in a new screen -->
      <a href="http://gravatar.com/emails" target="_blank">change</a>
    </div>
    
  </div>
</div>